# ---------------------------------------------------------------------------
# NOT FOR PUBLICATION
# ---------------------------------------------------------------------------
# Reverse-engineering working notes. Kept locally; deliberately not published.
ENHANCED_PORT.md

# Pre-refactor copy of the removed ReShade overlay. This project has no history
# to recover it from, which is why it exists at all - it is a local backup, not
# part of the mod.
_removed_overlay_backup/

# ---------------------------------------------------------------------------
# Build output
# ---------------------------------------------------------------------------
# The assembled release bundle. Everything in it is either a build output or a
# third-party binary (ffmpeg is ~100MB on its own), so it is packaged and
# attached to a GitHub release rather than tracked here.
dist/

build.vs/
build/
out/
x64/
*.asi
*.zip
*.lib
*.exp
*.pdb
*.ilk
*.map
*.obj

# ---------------------------------------------------------------------------
# Runtime artefacts - written next to the game, but easy to drag in by mistake
#
# The .ini files are GENERATED: their text lives in CMakeLists.txt and the build
# writes them next to the .asi. None of them belongs in the repo, wherever a
# copy turns up - a tracked one would drift from the generator and collect
# whatever settings were last tested in-game.
# ---------------------------------------------------------------------------
*.log
Captures/
markers/
ffmpeg.exe
RockstarEditorPlus.ini
Render.ini

# presets\ is a runtime folder next to the game, EXCEPT the shipped set under
# dist\ - the README names those five by name, so a clone needs them.
presets/
!dist/RockstarEditorPlus/presets/
!dist/RockstarEditorPlus/presets/*.ini

# ---------------------------------------------------------------------------
# Editor / OS
# ---------------------------------------------------------------------------
.vs/
.vscode/
CMakeSettings.json
*.user
Thumbs.db
desktop.ini
